# 最新修复进展核查（2026-09-20）

范围：结合任务 `019fe742-acce-7791-81fe-3ee08ae4d231`，只读核对当前源码、隔离容器和已落盘证据，补跑针对性测试。未修改业务代码、原件或交付包，未调用外部模型。

## 结论

部分修复已落地；9.20三个原件仍未通过。第二轮13页全部为partial，第二、第三份第3页均丢失第一轮已输出的表格。旧文本重新识别只完成了路径调整，内容完整性和结构恢复尚未解决。

## 当前状态与来源

- 原任务14:03:55中断，当前idle。容器`ai4med-integrity-readiness-0919`仍运行，但检查其/proc未发现`acceptance_0920.py`进程。
- 容器`/tmp/acceptance-0920/output/doc3-pages.json`及`summary.json`已在14:02:20写出，证明第二轮三个文件均有落盘结果。不是只完成前两份。
- 对解析器、表单解析器、审评服务、人工修订服务及两个关键测试文件进行字节比较，工作区与该隔离容器一致。此比较不代表整个部署环境或交付镜像一致。
- `pdf_page_extractor.py`最后修改时间为13:46:30，与旧OCR层修复对应；未发现该文件后续修复。

## 修复与验证

| 内容 | 当前证据 | 判定 |
| --- | --- | --- |
| 底纹小区域合并优化 | `pdf_page_extractor.py:611`按扩展框横轴排序并排除不相交候选；对话记录47项及87项回归通过 | 代码已落地；不能推断原件文字正确 |
| 不可见旧OCR层重新识别 | `pdf_page_extractor.py:1423`判定不可用，保留native_text和embedded_text_candidates，不再按旧词遮罩识别 | 路径修改已落地；真实识别仍失败 |
| 普通人工表格同步父字段 | `drug_supplement_pdf_parser.py:1123`保留表格Markdown、原有说明与来源区域 | 定向集成测试通过 |
| 审评前阻塞 | `filing_change_review_service.py:3347`创建运行记录前检查；3375处执行前复核 | 源码已存在；就绪判定定向测试通过 |
| 最新普通表格的浏览器闭环 | 现有table-route-browser记录为12:16，早于13:11之后的普通表格同步修改 | 不能当作这次最新修改的浏览器验收 |
| 并发 | current-final-pdf-performance证据9轮21次均成功，健康P95最大19.031ms；记录时间12:20 | 早于13:46修改，不证明最新扫描重识别路径性能；历史一次失败根因仍不明 |

本次执行：隔离容器内`python -m unittest test_pdf_page_extraction.PDFPagesTests.test_invisible_scan_text_is_not_used_to_mask_visible_source test_filing_readiness_integration.ReadinessIntegrationTests.test_data_table_revision_reaches_persisted_parent_field test_filing_parse_readiness`。结果11项、2.863秒、OK，无跳过。

测试边界：旧层用例替换OCR和表格识别函数，只证明不再用旧词遮罩。普通表格集成使用合成输入、隔离SQLite、真实启动/持久化/报告服务，模型编排为替身；证明修订值进入审评输入及Markdown/DOCX，不证明模型判断质量或真实扫描完整识别。对话中13:57的134项是历史组合回归，其中7跳过，不与本次11项累加计数。

## 三份原件的两轮差异

依据同一容器`/tmp/acceptance-0920/first-pass/docN-pages.json`与`output/docN-pages.json`。编号沿测试脚本对input目录PDF排序的编号；仅列统计，避免复制申报原文。

| 文件与页 | 第一轮字符数→第二轮 | 第一轮表格数→第二轮 | 第二轮状态 |
| --- | --- | --- | --- |
| 文档1第2页 | 1068→589 | 0→0 | partial |
| 文档2第3页 | 410→339 | 1→0 | partial |
| 文档3第3页 | 1381→555 | 1→0 | partial |

字符数变化仅用于定位，不能独立等同漏字比例；两张原已输出但仍待核对的表格变成无表格，是明确结构退步。13页第二轮均partial，没有整份验收通过证据。

`output/visual-label-check.json`时间为13:52，当时第二、第三份尚未全部重写，内容仍含旧native路径，与14:02第二轮结果不一致。它是两轮混合的中间核对产物，不作为最终验收依据；本次未修改它。

## 尚待解决

1. 新旧文字的可靠比较与融合，避免旧错字继续误用，也避免新OCR丢掉旧层中正确内容。
2. 表格几何与文字可信度分别处理。当前原生表格分支也受quality.usable控制（约1484行）；旧层置为不可用会跳过该分支。这是丢表的具体可疑机制，尚未通过针对性反例证明完整因果，不宣称已修复。
3. 用同一轮完整输出重新做逐页文字、表格、关键值核对，生成最终三文件验收清单；不能用partial或单测绿色代替。
4. 补最新普通表格修订的生产浏览器保存/刷新验证；历史并发失败仍保持原因未确认。

本次检查完成，业务修复尚未完成；没有替原任务恢复执行、发布或生成新交付包。
